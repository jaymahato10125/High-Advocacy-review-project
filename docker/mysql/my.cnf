# MySQL's innodb_ft_min_token_size defaults to 3, which would make a search for
# "G2" (2 chars, a real term in this domain) silently match nothing. Must be set
# before the FULLTEXT index is built — naturally satisfied by a fresh container.
[mysqld]
innodb_ft_min_token_size=2
